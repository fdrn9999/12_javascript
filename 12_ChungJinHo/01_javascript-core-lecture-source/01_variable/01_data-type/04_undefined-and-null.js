/* undefined
 * - 변수에 값이 저장되지 않ㄹ아서 타입이 결정되지 않은 상태
 * - 의도적으로 undefined를 대입하는 것은 지양한다.
 * */
var undef;
console.log(undef); // undefined

/* null
 * - 의도적으로 값이 없음을 나타내는 상태
 * - null은 undefined와 다르게 타입이 object로 결정된다.
 * */
var nullVar = null;
console.log(nullVar); // null

